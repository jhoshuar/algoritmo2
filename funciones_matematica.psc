Algoritmo funciones_matematica 
	definir valorabs, parteentera, redondear,raizc como real 
	escribir " ingrese el numero o calcular"
	leer numx
	valorabs = abs(numx)
	parteentera = trunc(numx) 
	redondear = redon(numx)
	raizc = raiz(numx)
	escribir " el valor absoluto de: " numx " es: " valorabs
	escribir " la part entera de: " numx " es: " parteentera
	escribir "  el numero: " numx " redondear es: " redondear
	escribir " la raiz cuadrada de: " numx " es: " raizc
FinAlgoritmo
